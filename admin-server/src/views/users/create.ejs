<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= title || "Create User" %> — Admin</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="/static/admin.css" rel="stylesheet" />
    <style>
      :root {
        --bg: #ffffff;
        --ink: #0b0b0b;
        --muted: #6b6b6b;
        --soft: #f7f7f7;
        --border: #e8e8e8;
        --brand-400: #e9953e;
        --brand-500: #d67a19;
        --brand-600: #b26014;
        --brand-900: #4b2808;
        --danger: #c62828;
        --radius: 16px;
        --shadow: 0 10px 30px rgba(0, 0, 0, 0.06);
      }
      html,
      body {
        height: 100%;
        margin: 0;
        font-family: "Segoe UI", "Roboto", Arial, sans-serif;
      }
      body,
      .app {
        background: var(--soft);
        color: var(--ink);
      }
      html,
      body {
        overflow: auto;
      }

      .cp-desktop-wrap {
        width: 100%;
        height: 100%;
        overflow: auto;
        background: var(--soft);
      }
      .cp-desktop {
        display: block;
        width: max-content;
        min-width: 100%;
        min-height: 100vh;
        margin: 0 auto;
        padding: 12px 0;
      }
      .cp-scale {
        transform-origin: top left;
        transform: scale(0.94);
      }
      @media (max-width: 1200px) {
        .cp-scale {
          transform: scale(0.9);
        }
      }
      @media (max-width: 992px) {
        .cp-scale {
          transform: scale(0.88);
        }
      }
      @media (max-width: 768px) {
        .cp-scale {
          transform: scale(0.86);
        }
      }
      @media (max-width: 600px) {
        .cp-scale {
          transform: scale(0.84);
        }
      }

      .container {
        background: var(--bg);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 24px 20px;
        margin: 0 auto;
        box-shadow: var(--shadow);
        max-width: 720px;
        width: 720px;
      }
      .title-large {
        font-size: 1.6rem;
        font-weight: 800;
        margin: 0 0 14px;
        color: var(--brand-900);
      }
      .mb-4 {
        margin-bottom: 1.2rem;
      }

      .alert {
        border-radius: 12px;
        padding: 10px 12px;
        font-size: 0.95rem;
        font-weight: 600;
        margin-bottom: 12px;
      }
      .alert-danger {
        background: #fdeaea;
        color: var(--danger);
        border: 1px solid #f5caca;
      }

      .form-group {
        margin-bottom: 14px;
      }
      .text-sm {
        font-size: 0.92rem;
      }
      .mb-1 {
        margin-bottom: 6px;
      }
      .mb-3 {
        margin-bottom: 14px;
      }
      .mb-4x {
        margin-bottom: 18px;
      }
      .mt-4 {
        margin-top: 18px;
      }
      .text-center {
        text-align: center;
      }

      label {
        display: block;
        font-weight: 700;
        color: var(--brand-900);
      }
      input[type="text"],
      input[type="email"],
      input[type="password"] {
        width: 100%;
        background: var(--bg);
        color: var(--ink);
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 12px 12px;
        font-size: 1rem;
        outline: none;
        transition: border-color 0.12s, box-shadow 0.12s;
      }
      input:focus {
        border-color: var(--brand-500);
        box-shadow: 0 0 0 3px rgba(214, 122, 25, 0.18);
      }

      .btn {
        display: inline-block;
        border: 1px solid var(--brand-600);
        background: var(--brand-500);
        color: #fff;
        font-weight: 800;
        letter-spacing: 0.02em;
        padding: 10px 18px;
        border-radius: 12px;
        cursor: pointer;
        text-decoration: none;
        transition: filter 0.15s ease;
      }
      .btn:hover {
        filter: brightness(0.96);
      }
      .btn:active {
        filter: brightness(0.92);
      }
      .btn-cancel {
        border: 1px solid #bbb;
        background: #e8e8e8;
        color: #333;
        margin-left: 8px;
      }
      .btn-cancel:hover {
        background: #ddd;
      }

      .px-5 {
        padding-left: 20px;
        padding-right: 20px;
      }
      .py-2 {
        padding-top: 10px;
        padding-bottom: 10px;
      }

      .form-row {
        display: grid;
        grid-template-columns: 1fr;
        gap: 12px;
      }
      @media (min-width: 700px) {
        .form-row {
          grid-template-columns: 1fr 1fr;
        }
      }
    </style>
  </head>
  <body class="app">
    <div class="cp-desktop-wrap">
      <div class="cp-desktop">
        <div class="cp-scale">
          <main class="container">
            <h1 class="title-large mb-4">Create User</h1>

            <% if (error) { %>
            <p class="alert alert-danger"><%= error %></p>
            <% } %>

            <form
              method="post"
              action="/admin/users/create/new"
              class="create-user-form"
              novalidate
            >
              <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <% } %>

              <fieldset>
                <legend>User Details</legend>

                <div class="form-group mb-3">
                  <label for="email" class="text-sm mb-1">Email</label>
                  <input
                    id="email"
                    name="email"
                    type="email"
                    required
                    placeholder="name@example.com"
                  />
                </div>

                <div class="form-group mb-3">
                  <label for="password" class="text-sm mb-1">Password</label>
                  <input
                    id="password"
                    name="password"
                    type="password"
                    required
                    placeholder="Minimum 8 characters"
                  />
                </div>

                <div class="form-row">
                  <div class="form-group">
                    <label for="first_name" class="text-sm mb-1"
                      >First name</label
                    >
                    <input
                      id="first_name"
                      name="first_name"
                      type="text"
                      placeholder="John"
                    />
                  </div>
                  <div class="form-group">
                    <label for="last_name" class="text-sm mb-1"
                      >Last name</label
                    >
                    <input
                      id="last_name"
                      name="last_name"
                      type="text"
                      placeholder="Doe"
                    />
                  </div>
                </div>

                <div class="form-group mb-4x">
                  <label for="username" class="text-sm mb-1">Username</label>
                  <input
                    id="username"
                    name="username"
                    type="text"
                    placeholder="johndoe"
                  />
                </div>

                <div class="form-group text-center mt-4">
                  <button class="btn px-5 py-2" type="submit">Create</button>
                  <button
                    type="button"
                    class="btn btn-cancel px-5 py-2"
                    onclick="history.back()"
                  >
                    Cancel
                  </button>
                </div>
              </fieldset>
            </form>
          </main>
        </div>
      </div>
    </div>
  </body>
</html>
